# A tea stall owner has a digital token display.
# For every customer in line, a token number is printed and chai is served.
# Task.
# .. Use for loop to genrate token no from 1 to 10 using range();
# .. Print: "Serving Chai to token no #{number}"

for token in range(1, 11):
    print(f"Serving chai to token no #{token}")
